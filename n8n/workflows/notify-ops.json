{
  "name": "Birdman — Notify Ops",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "ops/notify",
        "responseMode": "onReceived",
        "options": {}
      },
      "id": "webhook",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [260, 300]
    },
    {
      "parameters": {
        "functionCode": "const body = items[0].json.body || items[0].json;\nreturn [{ json: { message: body.payload?.message || body.topic, severity: body.payload?.severity || 'info', job_id: body.id } }];"
      },
      "id": "normalize",
      "name": "Normalize",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [520, 300]
    },
    {
      "parameters": {},
      "id": "discord",
      "name": "Discord / Slack (configure)",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [760, 300]
    }
  ],
  "connections": {
    "Webhook": { "main": [[{ "node": "Normalize", "type": "main", "index": 0 }]] },
    "Normalize": { "main": [[{ "node": "Discord / Slack (configure)", "type": "main", "index": 0 }]] }
  },
  "meta": {
    "birdman": { "topic": "ops.notify" }
  }
}
